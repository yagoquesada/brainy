import 'package:flutter/material.dart';

class YColors {
  // App theme colors
  static const Color primary = Color.fromARGB(255, 7, 206, 242);
  static const Color secondary = Color.fromARGB(255, 3, 85, 128);
  static const Color accent = Color(0xFFb0c7ff);

  // User msg - Background - TextField Colors
  static const Color scaffoldBackgroundColorLight = Color.fromARGB(255, 233, 233, 233);
  static const Color scaffoldBackgroundColorDark = Color(0xFF343541);

  static const Color backgroundColorLight = Color.fromARGB(255, 169, 169, 172);
  static const Color backgroundColorDark = Color.fromARGB(255, 38, 39, 43);

  // AppBar - Containter Colors
  static const Color cardColorLight = Color(0xFFFFFFFF);
  static const Color cardColorDark = Color(0xFF444654);

  // Bot msg
  static const Color messageColorLight = Color.fromARGB(255, 245, 245, 245);
  static const Color messageColorDark = Color(0xFF444654);

  // Icons in TextField Colors
  static const Color shadowColorLight = Colors.grey;
  static const Color shadowColorDark = Colors.grey;

  // Setting Screen BackGround
  static const Color scaffoldBackgroundColorLightSettings = Color.fromARGB(255, 245, 245, 245);
  static const Color scaffoldBackgroundColorDarkSettings = Color.fromARGB(255, 95, 95, 95);

  // Menu Background Color
  static const Color menuBackgroundColorDark = Color.fromARGB(255, 27, 27, 31);
  static const Color menuBackgroundColorLight = Color.fromARGB(255, 175, 175, 175);
}

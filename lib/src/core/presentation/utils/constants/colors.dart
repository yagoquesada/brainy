import 'package:flutter/material.dart';

class YColors {
  // App theme colors
  static const Color primary = Color.fromARGB(255, 7, 206, 242);
  static const Color secondary = Color.fromARGB(255, 3, 85, 128);
  static const Color accent = Color(0xFFb0c7ff);

  // User msg - Background - TextField Colors
  static const Color tScaffoldBackgroundColorLight = Color.fromARGB(255, 233, 233, 233);
  static const Color tScaffoldBackgroundColorDark = Color(0xFF343541);

  static const Color tBackgroundColorLight = Color.fromARGB(255, 169, 169, 172);
  static const Color tBackgroundColorDark = Color.fromARGB(255, 38, 39, 43);

  // AppBar - Containter Colors
  static const Color tCardColorLight = Color(0xFFFFFFFF);
  static const Color tCardColorDark = Color(0xFF444654);

  // Bot msg
  static const Color tMessageColorLight = Color.fromARGB(255, 245, 245, 245);
  static const Color tMessageColorDark = Color(0xFF444654);

  // Icons in TextField Colors
  static const Color tShadowColorLight = Colors.grey;
  static const Color tShadowColorDark = Colors.grey;

  // Setting Screen BackGround
  static const Color tScaffoldBackgroundColorLightSettings = Color.fromARGB(255, 245, 245, 245);
  static const Color tScaffoldBackgroundColorDarkSettings = Color.fromARGB(255, 95, 95, 95);

  // Menu Background Color
  static const Color tMenuBackgroundColorDark = Color.fromARGB(255, 27, 27, 31);
  static const Color tMenuBackgroundColorLight = Color.fromARGB(255, 175, 175, 175);
}
